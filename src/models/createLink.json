{
  "type": "object",
  "properties": {
    "longLink": {
      "type": "string",
      "pattern": "(https://www.|http://www.|https://|http://)?[a-zA-Z0-9]{2,}(.[a-zA-Z0-9]{2,})(.[a-zA-Z0-9]{2,})?"
    },
    "lifeTime": {
      "type": "string",
      "enum": ["singleuse", "oneday", "threedays", "sevendays"]
    }
  },
  "required": ["longLink", "lifeTime"]
}
